#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter two integers:");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf("Max of %d and %d is %d",a,b,a);
    else
    printf("Max of %d and %d is %d",a,b,a);
    return 0;
}
